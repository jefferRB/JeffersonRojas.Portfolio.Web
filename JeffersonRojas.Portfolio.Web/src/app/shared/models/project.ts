/**
 * Real projects referenced by the "Selected work" section.
 * Case studies are not part of this iteration; only the names and their
 * honest current status are shown.
 */
export type ProjectId = 'luxurycloud' | 'nexopos' | 'personalos';
